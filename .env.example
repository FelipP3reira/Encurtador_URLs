# Copie para .env e ajuste o que precisar.
NODE_ENV=development
PORT=3335

# URL pública usada para montar o link curto nas respostas.
BASE_URL=http://localhost:3335

# Postgres e Redis do docker-compose deste repositório.
DATABASE_URL=postgresql://encurtador:encurtador@localhost:5435/encurtador?schema=public
REDIS_URL=redis://localhost:6381
